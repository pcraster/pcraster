#define DEV_PATH_VARIABLE_SEPARATOR "${PATH_VARIABLE_SEPARATOR}"

#cmakedefine DEVBASE_GDAL_LIBRARY_HAS_OGR_SUPPORT
// #cmakedefine GDAL_LIBRARY_HAS_GDAL_DESTROY_DRIVER

#if defined(__GNUC__)
  #if defined(__GNUC_PATCHLEVEL__)
    #define __GNUC_VERSION__ (__GNUC__ * 10000 + __GNUC_MINOR__ * 100 + __GNUC_PATCHLEVEL__)
  #else
    #define __GNUC_VERSION__ (__GNUC__ * 10000 + __GNUC_MINOR__ * 100)
  # endif

  #if __GNUC_VERSION__ >= 40201
    #define GNUC_DIAGNOSTIC_PRAGMAS_SUPPORTED
  #endif
#endif

