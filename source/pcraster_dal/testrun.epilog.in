#vim:syntax=bash

# if [ $OSTYPE = "linux-gnu" ]
# then
#   if [ `which isql` ] # and $LOGNAME != kor ]
#   then
#     cat $DAL/Sources/PCRasterDal/deleteExampleTable | isql daltest $LOGNAME -b | grep --invert "SQLRowCount returns"
# 
#     # Update $HOME/.odbc.ini so information about the daltest DSN is removed.
#     odbcinst -u -s -n daltest | grep --invert "odbcinst: DSN removed (if it existed at all). ODBC_BOTH_DSN was used as the search path."
#   fi
# fi

# rm *

# No, keep the data.
# make --silent -f $DAL/Sources/PCRasterDal/Makefile.testrun clean

