- task ondersteunen elk een andere set van options, zet deze in de constructor of via een task member functie en maak een testOptions() oid, vergelijk met code in lumm.py voor testen options option in Global sectie.
  split: Skip
  indep: Skip
  infl: Skip
- excepties afvangen in initialise en run zodat de task waarvoor het misgaat genoemd kan worden
- als een commando faalt moet het commando gegeven worden en de data en scripts worden bewaard
- test validationSet optie met pcraster statement -> naamgeving conventie afspreken
- rename sampleMap naar modelSampleMap? of modelMap
  validationMap en modelMap
- dump uitvoer van alle commando in een log
  commando + meldingen
- moet split taak een split map opleveren of kan die weg?
- gebruik tempDirectory bij het aanroepen van tempnam voor tijdelijke bestanden.
- test of uitvoer files niet dezelfde naam hebben
- testjes schrijven
- all output moet in dezelfde directory terecht komen
  outputDirectory in de DEFAULT sectie zetten en in de uitvoer variabelen
  geen pad meer gebruiken
  geen van de uitvoer variabelen mag een directory bevatten
-   initialiase
    run
    clean
  maken?
  dan blijft de data staan als een exceptie wordt gegooid
- sample: 1307496 cellen
          1307409

- split map:
    modelSet
    modelSetSample
    validationSet
  en validation map en sample map eruit, split map moet alle kennis over sets
  bevatten en alle tasks kunnen die bevragen
- check grootte validatie set tov model set.

- dump verhouding in R script
- tabellen die te groot worden voor R opdelen in chunks.
    bla-1, bla-2, bla-3
    blaChunks = 3

- onderscheid model / validation uit de ini file
  vraag filename en splits zelf uit in model validation total sub dirs.

- constante kaarten geven problemen -> testen
